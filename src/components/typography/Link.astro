---
interface Props {
    href: string;
    target?: string;
    class?: string;
}

const { class: className, target, href } = Astro.props;
---

<a href={href} target={target} class:list={["text-lime-500 hover:text-lime-600", className]}>
    <slot />
</a>
